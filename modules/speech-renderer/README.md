# Speech Renderer
#
# Prompt-layer + post-processing pipeline that shapes LLM text into spoken
# register (fillers, hedges, elongations) before TTS.
#
# Implementation lives in speech_renderer.py (placeholder until Module 3).
